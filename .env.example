# Online backend (Cloudflare Worker). U dev režimu NIJE potreban —
# klijent podrazumevano gađa lokalni `pnpm cf:dev` (http://localhost:8787).
# Za produkcijski build (GH Pages) postavi URL deployovanog workera:
# VITE_API_URL=https://prefa-backend.<account>.workers.dev
VITE_API_URL=
